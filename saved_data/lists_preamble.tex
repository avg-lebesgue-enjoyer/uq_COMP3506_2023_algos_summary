\newcommand{\dataprintalgos}[1]{
	\par \textbf{\color{magenta}{\underline{Algorithms}}} this data structure(s) may utilise:
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:general_linear_structure}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Sort:
			\begin{itemize}[nosep]
				\item Comparison sort:
				\begin{itemize}[nosep]
					\item Selection sort (algo \ref{alg:selection_sort})
					\item Insertion sort (algo \ref{alg:insertion_sort})
					\item Merge sort (algo \ref{alg:merge_sort})
					\item Quick sort (algo \ref{alg:quick_sort})
					\item Heap Sort (algo \ref{alg:heap_sort})
				\end{itemize}
				\item Non-comparison sort:
				\begin{itemize}[nosep]
					\item Bucket sort (algo \ref{alg:bucket_sort})
					\item Lexicographic sort (algo \ref{alg:lexicographic_sort})
					\begin{itemize}[nosep]
						\item Radix sort (algo \ref{alg:radix_sort})
						\item Binary radix sort (algo \ref{alg:binary_radix_sort})
					\end{itemize}
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:static_sequence}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Sort:
			\begin{itemize}[nosep]
				\item Comparison sort:
				\begin{itemize}[nosep]
					\item Selection sort (algo \ref{alg:selection_sort})
					\item Insertion sort (algo \ref{alg:insertion_sort})
					\item Merge sort (algo \ref{alg:merge_sort})
					\item Quick sort (algo \ref{alg:quick_sort})
					\item Heap Sort (algo \ref{alg:heap_sort})
				\end{itemize}
				\item Non-comparison sort:
				\begin{itemize}[nosep]
					\item Bucket sort (algo \ref{alg:bucket_sort})
					\item Lexicographic sort (algo \ref{alg:lexicographic_sort})
					\begin{itemize}[nosep]
						\item Radix sort (algo \ref{alg:radix_sort})
						\item Binary radix sort (algo \ref{alg:binary_radix_sort})
					\end{itemize}
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:array}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Sort:
			\begin{itemize}[nosep]
				\item Comparison sort:
				\begin{itemize}[nosep]
					\item Selection sort (algo \ref{alg:selection_sort})
					\item Insertion sort (algo \ref{alg:insertion_sort})
					\item Merge sort (algo \ref{alg:merge_sort})
					\item Quick sort (algo \ref{alg:quick_sort})
					\item Heap Sort (algo \ref{alg:heap_sort})
				\end{itemize}
				\item Non-comparison sort:
				\begin{itemize}[nosep]
					\item Bucket sort (algo \ref{alg:bucket_sort})
					\item Lexicographic sort (algo \ref{alg:lexicographic_sort})
					\begin{itemize}[nosep]
						\item Radix sort (algo \ref{alg:radix_sort})
						\item Binary radix sort (algo \ref{alg:binary_radix_sort})
					\end{itemize}
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:dynamic_sequence}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Sort:
			\begin{itemize}[nosep]
				\item Comparison sort:
				\begin{itemize}[nosep]
					\item Selection sort (algo \ref{alg:selection_sort})
					\item Insertion sort (algo \ref{alg:insertion_sort})
					\item Merge sort (algo \ref{alg:merge_sort})
					\item Quick sort (algo \ref{alg:quick_sort})
					\item Heap Sort (algo \ref{alg:heap_sort})
				\end{itemize}
				\item Non-comparison sort:
				\begin{itemize}[nosep]
					\item Bucket sort (algo \ref{alg:bucket_sort})
					\item Lexicographic sort (algo \ref{alg:lexicographic_sort})
					\begin{itemize}[nosep]
						\item Radix sort (algo \ref{alg:radix_sort})
						\item Binary radix sort (algo \ref{alg:binary_radix_sort})
					\end{itemize}
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:linked_list}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Sort:
			\begin{itemize}[nosep]
				\item Comparison sort:
				\begin{itemize}[nosep]
					\item Selection sort (algo \ref{alg:selection_sort})
					\item Insertion sort (algo \ref{alg:insertion_sort})
					\item Merge sort (algo \ref{alg:merge_sort})
					\item Quick sort (algo \ref{alg:quick_sort})
					\item Heap Sort (algo \ref{alg:heap_sort})
				\end{itemize}
				\item Non-comparison sort:
				\begin{itemize}[nosep]
					\item Bucket sort (algo \ref{alg:bucket_sort})
					\item Lexicographic sort (algo \ref{alg:lexicographic_sort})
					\begin{itemize}[nosep]
						\item Radix sort (algo \ref{alg:radix_sort})
						\item Binary radix sort (algo \ref{alg:binary_radix_sort})
					\end{itemize}
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:extensible_list}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Sort:
			\begin{itemize}[nosep]
				\item Comparison sort:
				\begin{itemize}[nosep]
					\item Selection sort (algo \ref{alg:selection_sort})
					\item Insertion sort (algo \ref{alg:insertion_sort})
					\item Merge sort (algo \ref{alg:merge_sort})
					\item Quick sort (algo \ref{alg:quick_sort})
					\item Heap Sort (algo \ref{alg:heap_sort})
				\end{itemize}
				\item Non-comparison sort:
				\begin{itemize}[nosep]
					\item Bucket sort (algo \ref{alg:bucket_sort})
					\item Lexicographic sort (algo \ref{alg:lexicographic_sort})
					\begin{itemize}[nosep]
						\item Radix sort (algo \ref{alg:radix_sort})
						\item Binary radix sort (algo \ref{alg:binary_radix_sort})
					\end{itemize}
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:stack}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:queue}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:priority_queue}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:adaptable_priority_queue}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:tree}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:bintree}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:heap}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Heap Methods:
			\begin{itemize}[nosep]
				\item Upheap (algo \ref{alg:upheap})
				\item Downheap (algo \ref{alg:downheap})
				\item Heap.build() (algo \ref{alg:heap_build})
				\item Heap.insert() (algo \ref{alg:heap_insert})
				\item Heap.removeMin() (algo \ref{alg:heap_remove_min})
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{dat:bin_search_tree}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Search:
			\begin{itemize}[nosep]
				\item Binary search (algo \ref{alg:binsearch})
			\end{itemize}
			\item Binary Search Tree Methods:
			\begin{itemize}[nosep]
				\item BinSearchTree.remove() (algo \ref{alg:bin_search_tree_remove})
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{dat:avl_tree}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Search:
			\begin{itemize}[nosep]
				\item Binary search (algo \ref{alg:binsearch})
			\end{itemize}
			\item Binary Search Tree Methods:
			\begin{itemize}[nosep]
				\item BinSearchTree.remove() (algo \ref{alg:bin_search_tree_remove})
			\end{itemize}
			\item AVL Tree Methods:
			\begin{itemize}[nosep]
				\item AVLTree.insert(), AVLTree.remove() (algo \ref{alg:avl_tree_insert_remove})
				\item AVLTree.rebalance(), AVLTree.triNodeRestructuring() (algo \ref{alg:avl_tree_rebalance})
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{dat:splay_tree}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Search:
			\begin{itemize}[nosep]
				\item Binary search (algo \ref{alg:binsearch})
			\end{itemize}
			\item Binary Search Tree Methods:
			\begin{itemize}[nosep]
				\item BinSearchTree.remove() (algo \ref{alg:bin_search_tree_remove})
			\end{itemize}
			\item Splay Tree Methods:
			\begin{itemize}[nosep]
				\item SplayTree.splay() (algo \ref{alg:splay})
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{dat:multi_way_search_tree}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{nts:red_black_tree}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{nts:b_tree}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:set}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:map}
		}
	}{ % IF
		(none yet)
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{adt:graph}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Graph Algorithms:
			\begin{itemize}[nosep]
				\item Graph Traversals:
				\begin{itemize}[nosep]
					\item Breadth-first search (algo \ref{alg:bfs})
					\item Depth-first search (algo \ref{alg:dfs})
				\end{itemize}
				\item Graph Problem Solvers:
				\begin{itemize}[nosep]
					\item Dijkstra (algo \ref{alg:dijkstra})
					\item Single source shortest path via topological sort (algo \ref{alg:dag_distances})
					\item Strong connectivity solver (algo \ref{alg:strong_connectivity_solver})
					\item Topological sort (algo \ref{alg:topological_sort})
					\item Cycle detection via DFS (algo \ref{alg:cycle_dfs})
					\item Prim-Jarnik (algo \ref{alg:prim_jarnik})
					\item Kruskal (algo \ref{alg:kruskal})
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{dat:edge_list_graph}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Graph Algorithms:
			\begin{itemize}[nosep]
				\item Graph Traversals:
				\begin{itemize}[nosep]
					\item Breadth-first search (algo \ref{alg:bfs})
					\item Depth-first search (algo \ref{alg:dfs})
				\end{itemize}
				\item Graph Problem Solvers:
				\begin{itemize}[nosep]
					\item Dijkstra (algo \ref{alg:dijkstra})
					\item Single source shortest path via topological sort (algo \ref{alg:dag_distances})
					\item Strong connectivity solver (algo \ref{alg:strong_connectivity_solver})
					\item Topological sort (algo \ref{alg:topological_sort})
					\item Cycle detection via DFS (algo \ref{alg:cycle_dfs})
					\item Prim-Jarnik (algo \ref{alg:prim_jarnik})
					\item Kruskal (algo \ref{alg:kruskal})
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{dat:adjacency_list_graph}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Graph Algorithms:
			\begin{itemize}[nosep]
				\item Graph Traversals:
				\begin{itemize}[nosep]
					\item Breadth-first search (algo \ref{alg:bfs})
					\item Depth-first search (algo \ref{alg:dfs})
				\end{itemize}
				\item Graph Problem Solvers:
				\begin{itemize}[nosep]
					\item Dijkstra (algo \ref{alg:dijkstra})
					\item Single source shortest path via topological sort (algo \ref{alg:dag_distances})
					\item Strong connectivity solver (algo \ref{alg:strong_connectivity_solver})
					\item Topological sort (algo \ref{alg:topological_sort})
					\item Cycle detection via DFS (algo \ref{alg:cycle_dfs})
					\item Prim-Jarnik (algo \ref{alg:prim_jarnik})
					\item Kruskal (algo \ref{alg:kruskal})
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
	\ifthenelse{
		\equal{
			#1
		}{
			\ref{dat:adjacency_matrix_graph}
		}
	}{ % IF
		\begin{itemize}[nosep]
			\item Graph Algorithms:
			\begin{itemize}[nosep]
				\item Graph Traversals:
				\begin{itemize}[nosep]
					\item Breadth-first search (algo \ref{alg:bfs})
					\item Depth-first search (algo \ref{alg:dfs})
				\end{itemize}
				\item Graph Problem Solvers:
				\begin{itemize}[nosep]
					\item Dijkstra (algo \ref{alg:dijkstra})
					\item Single source shortest path via topological sort (algo \ref{alg:dag_distances})
					\item Strong connectivity solver (algo \ref{alg:strong_connectivity_solver})
					\item Topological sort (algo \ref{alg:topological_sort})
					\item Cycle detection via DFS (algo \ref{alg:cycle_dfs})
					\item Prim-Jarnik (algo \ref{alg:prim_jarnik})
					\item Kruskal (algo \ref{alg:kruskal})
				\end{itemize}
			\end{itemize}
		\end{itemize}
	}{} % empty ELSE block
}